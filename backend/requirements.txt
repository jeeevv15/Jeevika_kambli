# API layer
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0

# Shared with the closed-loop pipeline (see root requirements.txt) —
# duplicated here so `pip install -r backend/requirements.txt` alone
# is enough to run the API.
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
xgboost>=2.0.0
